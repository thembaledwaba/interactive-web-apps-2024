const nickname= "Timmy";
const firstname = "Timothy";
const greeting = nickname || firstname || "!";             // added a const for the greeting for readability 
console.log(`Good morning ${greeting}`)               // used the correct quotes
	
